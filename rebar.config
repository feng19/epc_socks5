{erl_opts, [no_debug_info]}.

{shell, [
    {apps, [epc_socks5]},
    {config, "client.config"}
]}.

{escript_name, "epc"}.
{escript_incl_apps, []}.
{escript_emu_args, "%%! +K true -config client -escript main epc_socks5_app\n"}.

{deps, []}.
